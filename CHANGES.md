# DiskWise 改进记录

## 2026-08-30 重大更新

### 1. 搜索引擎增强

#### 路径过滤
- 添加了 `path_filter` 参数到搜索方法
- 支持按目录路径前缀过滤搜索结果
- 文件名搜索和全文搜索都支持此功能

#### 多扩展名过滤
- 支持逗号分隔的多个扩展名（如 `.pdf,.docx,.xlsx`）
- 使用 SQL `IN` 子句提高查询效率

### 2. UI 界面改进

#### 搜索范围选择
新增搜索范围下拉框，包含三个选项：
- 🌐 **全局搜索**（默认）：搜索所有已索引的文件
- 📂 **当前目录**：只搜索当前浏览的目录及其子目录
- 📂 **选择目录...**：弹出文件夹选择对话框，搜索指定目录

#### 文件格式过滤
新增文件格式下拉框，快速过滤特定类型的文件：
- 全部格式
- 📄 文档 (PDF/Word/Excel/PPT/TXT)
- 🖼 图片 (JPG/PNG/GIF/BMP/WEBP)
- 🎬 视频 (MP4/AVI/MKV/MOV/WMV)
- 🎵 音频 (MP3/WAV/FLAC/AAC/OGG)
- 📦 压缩包 (ZIP/RAR/7Z/TAR/GZ)
- ⚙️ 自定义扩展名...（弹出输入对话框，支持逗号分隔多个扩展名）

#### 索引管理改进
将单个"构建索引"按钮改为两个独立按钮：

1. **🔄 刷新索引**
   - 执行增量更新
   - 只处理新增或修改的文件
   - 速度快，适合日常使用
   - 索引存在时可用

2. **🔨 重建索引**
   - 强制重建整个索引
   - 删除旧索引并重新扫描所有文件
   - 会弹出确认对话框防止误操作
   - 索引存在时可用

#### 索引状态显示优化
- **已索引状态**：显示"索引已就绪：X 个文件/文档 · 上次更新 YYYY-MM-DD HH:MM"
- **未索引状态**：显示"尚未构建索引，点击「构建索引」开始"
- 清晰展示索引数量和最后更新时间

### 3. 索引持久化

#### 增量更新机制
- 文件名索引：检查文件的修改时间（mtime），只更新变化的文件
- 全文索引：同样基于 mtime 进行增量更新
- 首次索引后，后续刷新只需几秒钟

#### 索引存储位置
- 文件名索引：`~/.diskwise/index.db`
- 全文索引：`~/.diskwise/fulltext.db`
- 索引在应用重启后仍然保留

### 4. 搜索引擎简化

#### 必应搜索
- 移除了百度和谷歌搜索引擎选项
- 只保留必应搜索（中国大陆可直接访问）
- 简化了 `WebSearch` 类的实现

### 5. 文件预览功能

#### 详情页面预览
- 在文件详情页面右侧添加了文档预览面板
- 支持预览 PDF、Word、Excel、PPT、TXT 等格式
- 点击左侧文件列表时自动更新预览内容
- 文件夹显示"（文件夹无法预览）"提示

#### 搜索结果预览
- 搜索结果页面右侧显示文档预览
- 内容搜索显示匹配片段（snippet）
- 文件名搜索显示完整文档内容

### 6. 界面布局优化

#### 去除 Tab 栏
- 移除了顶部的"文件详情/空间扫描/快速搜索"标签页
- 使用工具栏按钮直接切换视图
- 界面更加简洁直观

#### 磁盘使用情况
- 从右侧面板移到底部横向条
- 每个磁盘显示：盘符 + 使用量/总量 + 百分比 + 进度条
- 进度条颜色根据使用率变化（蓝色 < 75%，橙色 75-90%，红色 > 90%）

### 7. 技术改进

#### 代码质量
- 添加了 `QInputDialog` 导入用于自定义扩展名输入
- 修复了所有对已删除 `_main_tabs` 的引用
- 统一了按钮状态管理逻辑

#### 性能优化
- 增量索引避免重复扫描未修改的文件
- 多扩展名过滤使用 SQL `IN` 子句
- 路径过滤使用 SQL `LIKE` 子句

## 使用指南

### 首次使用
1. 启动应用后，点击工具栏的"🔍 快速搜索"按钮
2. 点击"🔄 刷新索引"或"🔨 重建索引"构建索引
3. 索引构建完成后，即可开始搜索

### 日常使用
1. **文件名搜索**：
   - 选择搜索模式为"文件名搜索"
   - 输入关键词（支持通配符 `*` 和 `?`）
   - 可选择搜索范围和文件格式进行过滤

2. **内容搜索**：
   - 选择搜索模式为"内容搜索"
   - 输入要搜索的文本内容
   - 可选择搜索范围和文件格式进行过滤

3. **索引维护**：
   - 日常使用"🔄 刷新索引"（增量更新，快速）
   - 索引损坏或需要完全重建时使用"🔨 重建索引"

### 过滤示例
- 搜索范围：当前目录 → 只搜索 C:\Users\JackA\Documents
- 文件格式：文档 → 只搜索 .pdf,.docx,.xlsx,.pptx,.txt 文件
- 自定义扩展名：输入 `.log,.bak` → 只搜索日志和备份文件

## 文件变更清单

### 新增功能
- `src/main_window.py`: 添加搜索范围、文件格式过滤、索引管理按钮
- `src/quick_search.py`: 添加 `path_filter` 参数、多扩展名支持、增量索引
- `src/fulltext_search.py`: 添加增量索引支持
- `src/web_search.py`: 简化为只支持必应搜索

### 修改文件
- `src/main_window.py`: 重构 UI 布局，去除 Tab 栏，添加预览面板
- `requirements.txt`: 添加 pypdf, python-docx, openpyxl, python-pptx, striprtf 依赖

## 测试验证

所有修改已通过以下验证：
- ✅ Python 语法检查（py_compile）
- ✅ 模块导入测试
- ✅ 搜索功能测试
- ✅ 内容提取测试
- ✅ GUI 启动测试

## 后续优化建议

1. **文件监控**：使用 Windows ReadDirectoryChangesW API 实时监控文件变化
2. **索引压缩**：对大型索引数据库进行压缩优化
3. **搜索历史**：保存用户的搜索历史，方便快速重复搜索
4. **高级搜索语法**：支持更复杂的搜索语法（如 AND、OR、NOT）
5. **批量操作**：支持批量删除、批量移动搜索结果
